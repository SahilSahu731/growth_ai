# Neon PostgreSQL connection string
DATABASE_URL="postgresql://neondb_owner:npg_dfXRb4LMZ3hu@ep-curly-smoke-ad13sm9z-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="replace-with-a-long-random-secret"

# Optional alias used by this project (falls back to NEXTAUTH_SECRET)
AUTH_SECRET="replace-with-a-long-random-secret"

# App URL for auth callbacks in local development
NEXTAUTH_URL="http://localhost:3000"

# Optional OAuth provider
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
# Optional aliases also supported:
# GOOGLE_ID=""
# GOOGLE_SECRET=""
