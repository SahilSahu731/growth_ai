# Third-party notices

GrowthAI includes open-source dependencies under their respective licenses. Production dependency licenses are checked in CI with `npm run license:check`.

- Fontsource font packages include font files under the SIL Open Font License 1.1 (OFL-1.1). Copyright and license details remain in each installed package.
- Next.js image optimization can distribute prebuilt `sharp`/`libvips` components under LGPL-3.0-or-later. GrowthAI uses the unmodified dynamically linked/prebuilt component; corresponding source and license information are available from the package's upstream distribution.
- Browser compatibility data from `caniuse-lite` is licensed under CC-BY-4.0; attribution and upstream authorship are retained in that package.

This notice supplements, and does not replace, license files shipped with dependencies.
